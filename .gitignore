# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Env files — layered system (vben-admin style)
# Committed (safe — no secrets):
#   .env                  shared base, all apps, all envs
#   .env.development      dev overrides per app
#   .env.production       prod overrides per app
# Gitignored (may contain secrets / machine-local values):
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
backup
.pnpm-store